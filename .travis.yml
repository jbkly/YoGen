language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-yogen' ]; then cd .. && eval "mv $currentfolder generator-yogen" && cd generator-yogen; fi

